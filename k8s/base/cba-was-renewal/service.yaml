apiVersion: v1
kind: Service
metadata:
  name: cba-was-renewal
  labels:
    app: cba-was-renewal
spec:
  type: ClusterIP
  selector:
    app: cba-was-renewal
  ports:
    - name: http
      port: 3000
      targetPort: 3000
