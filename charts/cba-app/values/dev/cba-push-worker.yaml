fullnameOverride: cba-was-renewal-push-worker

replicaCount: 1

image:
  repository: ap-chuncheon-1.ocir.io/axdhp42jvukm/cba_was_renew
  tag: dev-202607101335-bb3dc8e
  pullPolicy: Always

imagePullSecrets:
  - name: ocir-secret

serviceAccount:
  create: false
  name: ""

container:
  name: push-worker
  port: 3000
  portName: http

command:
  - node
args:
  - dist/apps/push-worker/apps/push-worker/src/main.js

envFrom:
  - secretRef:
      name: cba-was-renewal-env

service:
  enabled: false

livenessProbe:
  enabled: false

readinessProbe:
  enabled: false

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 512Mi

ingress:
  enabled: false
